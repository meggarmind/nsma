# NSMA Docker Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# =====================================
# Notion API Configuration
# =====================================

# Your Notion integration token
# Get it from: https://www.notion.so/my-integrations
# Format: secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NOTION_TOKEN=ntn_your_token_here

# Your Notion database ID
# Find it in the database URL: https://notion.so/{workspace}/{database_id}?v=...
# Format: 32-character hex string
NOTION_DATABASE_ID=your_database_id_here

# =====================================
# NSMA Registration API
# =====================================

# Registration token for project self-registration
# Generate one in Settings UI or use: uuidgen (Linux/Mac) or [guid]::NewGuid() (PowerShell)
# Format: UUID v4
REGISTRATION_TOKEN=your_uuid_token_here

# =====================================
# Paths (for Docker deployment)
# =====================================

# Host path to projects directory (bind mount to container /projects)
# This is where your project prompts folders are located
# Relative paths are relative to this docker-compose.yml file
# Examples:
#   - ./projects (default - projects folder in same directory as docker-compose.yml)
#   - /home/user/projects (absolute path)
#   - ../projects (relative path to parent directory)
PROJECTS_PATH=./projects

# =====================================
# Network Configuration
# =====================================

# Comma-separated list of allowed origins for server actions
# Add your production domain(s) here
# Examples:
#   - localhost:3100 (default for local development)
#   - nsma.example.com,api.example.com (production domains)
ALLOWED_ORIGINS=localhost:3100

# =====================================
# Optional: Advanced Configuration
# =====================================

# Override default config directory (default: /config in container)
# Only change this if you have specific requirements
# NOTION_SYNC_CONFIG_DIR=/custom/config/path

# Override default projects base path (default: /projects in container)
# Only change this if you have specific requirements
# PROJECTS_BASE_PATH=/custom/projects/path
